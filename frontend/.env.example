# Backend API base URL (must include /api suffix)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Session cookie max-age in seconds (match backend JWT_EXPIRES_IN_SECONDS)
NEXT_PUBLIC_SESSION_MAX_AGE_SECONDS=3600
